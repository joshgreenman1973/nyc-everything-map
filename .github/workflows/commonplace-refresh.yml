name: CommonPlace data refresh

# Monthly rebuild of docs/data/commonplace.json from CommonPlace (t95h-5fsr),
# the gazetteer behind the city's geocoder. The source is refreshed weekly by
# the Office of Technology and Innovation; monthly is plenty for a naming layer
# and keeps the diff noise down.
#
# The build script fails loudly on an empty or suspiciously short response
# rather than writing a truncated file, so a bad upstream day leaves the last
# good build in place and surfaces as a failed run (GitHub emails Josh).

on:
  schedule:
    - cron: '15 11 2 * *'    # 2nd of the month, ~06:15 ET
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Rebuild CommonPlace data
        run: python3 scripts/build_commonplace.py
      - name: Commit if the places changed
        run: |
          git config user.name "Josh Greenman"
          git config user.email "josh.greenman@gmail.com"
          # Ignore a same-data rebuild: the only diff would be the "built" date.
          if git diff --quiet -- docs/data/commonplace.json; then
            echo "no change"; exit 0
          fi
          python3 - <<'PY'
          import json, subprocess
          new = json.load(open('docs/data/commonplace.json'))
          old = json.loads(subprocess.run(
              ['git', 'show', 'HEAD:docs/data/commonplace.json'],
              capture_output=True, text=True).stdout or '{}')
          same = all(new.get(k) == old.get(k) for k in ('pts', 'types', 'counts'))
          open('.cp_changed', 'w').write('0' if same else str(len(new['pts'])))
          PY
          n=$(cat .cp_changed); rm -f .cp_changed
          if [ "$n" = "0" ]; then
            echo "only the build date moved; not committing"
            git checkout -- docs/data/commonplace.json
            exit 0
          fi
          git add docs/data/commonplace.json
          git commit -m "Refresh CommonPlace named places ($n places) [skip ci]"
          git push origin HEAD:main
